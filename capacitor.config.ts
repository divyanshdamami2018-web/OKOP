import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.okops.app',
  appName: "OKOP'S",
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
